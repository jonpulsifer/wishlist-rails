<% provide(:title, "Wishlist!") %>

<div class="container">
  <%= render "navbar", locals: { user: @user } %>

  <div class="row">
    <div class="col shadow-sm">
      <%= flash_messages %>rwarwrarwaarw
      <% @wishlist.each do |wl| %>
          <%= render 'card-wishlist', user: wl.user %>
      <% end %>
      lolrender partial: "wl_table", locals: { wishlists: Array(@wishlist) } 
    </div>
  </div>
</div>
