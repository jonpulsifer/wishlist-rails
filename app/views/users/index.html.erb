<% provide(:title, "Wishlist!") %>
<div class="mdl-grid">
  <%= flash_messages %>
  <% @users.each do |u| %>
      <% if u.id != current_user.id %>
        <%= render "card-user", user: u %>
      <% end %>
  <% end %>
</div>
