<% provide(:title, @gift.name) %>
<%= flash_messages %>
<div class="page-content">
  <div class="mdl-grid">
    <%= render 'card-gift', gift: @gift %>
    <%= render 'card-wishlist', user: @gift.user %>
  </div>
</div>
